- break basic testing via files to:
    - file and dir logic tests with real files
    - unit test for all other parts
- added mail validation but maybe remove (all invalid users will pass if i will not throw them, extra logic not in assignment)
